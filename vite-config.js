// vite.config.js
export default {
    // Specify the root directory for your project (optional)
    root: './bin',
  
    // Specify the build output directory (optional)
    build: {
      outDir: '../public', // Assuming your Express server serves static files from the 'public' directory
    },
  };
  